// TODO: change this code
let myMe = {
  firstName: "Jack",
  lastName: "Smith",
};
(myMe.age = 19), (employed = true);
{
  for (let member in myMe)
    if (myMe.hasOwnProperty[member]) {
      console.log(member + myMe[member]);
    }
}
